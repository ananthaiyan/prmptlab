version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: promptbench-db
    environment:
      POSTGRES_USER: promptbench
      POSTGRES_PASSWORD: promptbench
      POSTGRES_DB: promptbench
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U promptbench"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
